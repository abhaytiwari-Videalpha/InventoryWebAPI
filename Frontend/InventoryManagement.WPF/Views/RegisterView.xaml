<UserControl x:Class="InventoryManagement.WPF.Views.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:InventoryManagement.WPF.Converters"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:InverseBooleanConverter x:Key="InverseBooleanConverter" />
    </UserControl.Resources>

    <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Width="360">
        <StackPanel>
            <TextBlock Text="Create Account"
                       FontSize="26"
                       FontWeight="Bold"
                       Foreground="{StaticResource TextPrimaryBrush}"
                       Margin="0,0,0,24" />

            <TextBlock Text="Email" Margin="0,0,0,4" Foreground="{StaticResource TextSecondaryBrush}" />
            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />

            <TextBlock Text="Password" Margin="0,0,0,4" Foreground="{StaticResource TextSecondaryBrush}" />
            <PasswordBox x:Name="PasswordBoxControl"
                         PasswordChanged="PasswordBoxControl_PasswordChanged"
                         Margin="0,0,0,12" />

            <TextBlock Text="Confirm Password" Margin="0,0,0,4" Foreground="{StaticResource TextSecondaryBrush}" />
            <PasswordBox x:Name="ConfirmPasswordBoxControl"
                         PasswordChanged="ConfirmPasswordBoxControl_PasswordChanged"
                         Margin="0,0,0,4" />

            <TextBlock Text="{Binding ErrorMessage}"
                       Foreground="{StaticResource ErrorBrush}"
                       TextWrapping="Wrap"
                       Margin="0,4,0,4"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource BoolToVisibilityConverter}}" />

            <TextBlock Text="{Binding SuccessMessage}"
                       Foreground="{StaticResource SuccessBrush}"
                       TextWrapping="Wrap"
                       Margin="0,4,0,4"
                       Visibility="{Binding SuccessMessage, Converter={StaticResource BoolToVisibilityConverter}}" />

            <Button Content="Register"
                    Command="{Binding RegisterCommand}"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}"
                    Margin="0,12,0,8"
                    HorizontalAlignment="Stretch" />

            <TextBlock HorizontalAlignment="Center" Margin="0,8,0,0">
                <Run Text="Already have an account?" Foreground="{StaticResource TextSecondaryBrush}" />
                <Hyperlink Command="{Binding NavigateToLoginCommand}">
                    <Run Text="Sign In" />
                </Hyperlink>
            </TextBlock>
        </StackPanel>
    </Grid>
</UserControl>