<UserControl x:Class="InventoryManagement.WPF.Views.ProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="clr-namespace:InventoryManagement.WPF.Converters"
             mc:Ignorable="d">

    <UserControl.Resources>
        <converters:ProductStatisticsConverter x:Key="ProductStatisticsConverter" />
        <converters:ProductListEmptyConverter x:Key="ProductListEmptyConverter" />
        <converters:LowStockForegroundConverter x:Key="LowStockForegroundConverter" />
        <converters:BoolToVisibilityConverter x:Key="StringVisibilityConverter" />
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- ===================== PAGE HEADER ===================== -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="{Binding Title}" FontSize="26" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" />
            <TextBlock Text="Manage inventory products and stock levels"
                       FontSize="13"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       Margin="0,4,0,0" />
        </StackPanel>

        <!-- ===================== ERROR BANNER ===================== -->
        <Border Grid.Row="1"
                Background="{StaticResource ErrorSoftBrush}"
                BorderBrush="{StaticResource ErrorBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="14,10"
                Margin="0,0,0,16"
                Visibility="{Binding ErrorMessage, Converter={StaticResource StringVisibilityConverter}}">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource ErrorBrush}" TextWrapping="Wrap" FontWeight="SemiBold" />
        </Border>

        <!-- ===================== KPI CARDS ===================== -->
        <Grid Grid.Row="2" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Total Products -->
            <Border Grid.Column="0" Style="{StaticResource KpiCardStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Border Width="44" Height="44" CornerRadius="22" Background="{StaticResource AccentSoftBrush}" VerticalAlignment="Top">
                        <TextBlock Text="&#xE7B8;" FontFamily="Segoe MDL2 Assets" FontSize="18"
                                   Foreground="{StaticResource AccentBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <StackPanel Grid.Column="1" Margin="14,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="Total Products" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" />
                        <TextBlock FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,4,0,0">
                            <TextBlock.Text>
                                <MultiBinding Converter="{StaticResource ProductStatisticsConverter}" ConverterParameter="Count">
                                    <Binding Path="Products" />
                                    <Binding Path="IsBusy" />
                                </MultiBinding>
                            </TextBlock.Text>
                        </TextBlock>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Total Inventory Value -->
            <Border Grid.Column="2" Style="{StaticResource KpiCardStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Border Width="44" Height="44" CornerRadius="22" Background="{StaticResource SuccessSoftBrush}" VerticalAlignment="Top">
                        <TextBlock Text="&#xE8C7;" FontFamily="Segoe MDL2 Assets" FontSize="18"
                                   Foreground="{StaticResource SuccessBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <StackPanel Grid.Column="1" Margin="14,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="Total Inventory Value" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" />
                        <TextBlock FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,4,0,0">
                            <TextBlock.Text>
                                <MultiBinding Converter="{StaticResource ProductStatisticsConverter}" ConverterParameter="Value">
                                    <Binding Path="Products" />
                                    <Binding Path="IsBusy" />
                                </MultiBinding>
                            </TextBlock.Text>
                        </TextBlock>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Low Stock Items -->
            <Border Grid.Column="4" Style="{StaticResource KpiCardStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Border Width="44" Height="44" CornerRadius="22" Background="{StaticResource WarningSoftBrush}" VerticalAlignment="Top">
                        <TextBlock Text="&#xE7BA;" FontFamily="Segoe MDL2 Assets" FontSize="18"
                                   Foreground="{StaticResource WarningBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <StackPanel Grid.Column="1" Margin="14,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="Low Stock Items" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" />
                        <TextBlock FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,4,0,0">
                            <TextBlock.Text>
                                <MultiBinding Converter="{StaticResource ProductStatisticsConverter}" ConverterParameter="LowStock">
                                    <Binding Path="Products" />
                                    <Binding Path="IsBusy" />
                                </MultiBinding>
                            </TextBlock.Text>
                        </TextBlock>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Average Product Price -->
            <Border Grid.Column="6" Style="{StaticResource KpiCardStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Border Width="44" Height="44" CornerRadius="22" Background="{StaticResource AccentSoftBrush}" VerticalAlignment="Top">
                        <TextBlock Text="&#xE8EA;" FontFamily="Segoe MDL2 Assets" FontSize="18"
                                   Foreground="{StaticResource AccentBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <StackPanel Grid.Column="1" Margin="14,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="Average Product Price" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" />
                        <TextBlock FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,4,0,0">
                            <TextBlock.Text>
                                <MultiBinding Converter="{StaticResource ProductStatisticsConverter}" ConverterParameter="AveragePrice">
                                    <Binding Path="Products" />
                                    <Binding Path="IsBusy" />
                                </MultiBinding>
                            </TextBlock.Text>
                        </TextBlock>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <!-- ===================== TOOLBAR ===================== -->
        <Grid Grid.Row="3" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Background="{StaticResource SurfaceBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="12,0"
                    Width="320"
                    HorizontalAlignment="Left">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="&#xE721;" FontFamily="Segoe MDL2 Assets" FontSize="14"
                               Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="0,0,8,0" />
                    <TextBox Grid.Column="1"
                             Style="{StaticResource SearchTextBoxStyle}"
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Enter" Command="{Binding SearchCommand}" />
                        </TextBox.InputBindings>
                    </TextBox>
                </Grid>
            </Border>

            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Search" Command="{Binding SearchCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,8,0" />
                <Button Content="Filter" Command="{Binding FilterCommand}" Style="{StaticResource SecondaryButtonStyle}"
                     Margin="0,0,8,0"/>
                <Button Content="Refresh" Command="{Binding RefreshCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,8,0" />
                <Button Content="+ Add Product" Command="{Binding AddProductCommand}" Style="{StaticResource PrimaryCtaButtonStyle}" />
            </StackPanel>
        </Grid>

        <!-- ===================== TABLE + EMPTY STATE + LOADING OVERLAY ===================== -->
        <Grid Grid.Row="4">
            <Border Background="{StaticResource SurfaceBrush}" CornerRadius="12">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="20" ShadowDepth="2" Opacity="0.06" Color="Black" />
                </Border.Effect>

                <Grid>
                    <DataGrid Style="{StaticResource ModernDataGridStyle}"
                            ItemsSource="{Binding Products}"
                            SelectedItem="{Binding SelectedProduct}"
                            AutoGenerateColumns="False"
                            CanUserAddRows="False"
                            IsReadOnly="True">
                        <DataGrid.Resources>
                            <Style TargetType="ScrollBar">
                                <Setter Property="Width" Value="8" />
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ScrollBar">
                                            <Grid>
                                                <Track Name="PART_Track" IsDirectionReversed="True">
                                                    <Track.Thumb>
                                                        <Thumb>
                                                            <Thumb.Template>
                                                                <ControlTemplate TargetType="Thumb">
                                                                    <Border Background="{StaticResource ScrollBarThumbBrush}"
                                                                            CornerRadius="4"
                                                                            Width="6"
                                                                            HorizontalAlignment="Center" />
                                                                </ControlTemplate>
                                                            </Thumb.Template>
                                                        </Thumb>
                                                    </Track.Thumb>
                                                    <Track.DecreaseRepeatButton>
                                                        <RepeatButton Background="Transparent" BorderThickness="0" Opacity="0" />
                                                    </Track.DecreaseRepeatButton>
                                                    <Track.IncreaseRepeatButton>
                                                        <RepeatButton Background="Transparent" BorderThickness="0" Opacity="0" />
                                                    </Track.IncreaseRepeatButton>
                                                </Track>
                                            </Grid>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGrid.Resources>
                        <DataGrid.Visibility>
                            <MultiBinding Converter="{StaticResource ProductListEmptyConverter}" ConverterParameter="Invert">
                                <Binding Path="Products" />
                                <Binding Path="IsBusy" />
                            </MultiBinding>
                        </DataGrid.Visibility>

                        <DataGrid.Columns>

                            <DataGridTextColumn Header="ID" Binding="{Binding ProductId}" Width="80" />

                            <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontWeight" Value="SemiBold" />
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat={}{0:N2}}" Width="140">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="TextAlignment" Value="Right" />
                                        <Setter Property="HorizontalAlignment" Value="Stretch" />
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="120">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="TextAlignment" Value="Right" />
                                        <Setter Property="HorizontalAlignment" Value="Stretch" />
                                        <Setter Property="FontWeight" Value="SemiBold" />
                                        <Setter Property="Foreground" Value="{Binding Quantity, Converter={StaticResource LowStockForegroundConverter}}" />
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTemplateColumn Header="" Width="70">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="&#x22EE;"
                                                Style="{StaticResource RowActionsButtonStyle}"
                                                Click="RowActionsButton_Click"
                                                HorizontalAlignment="Center">
                                            <Button.ContextMenu>
                                                <ContextMenu>
                                                    <MenuItem Header="Edit Product" Click="EditMenuItem_Click" />
                                                    <MenuItem Header="Delete Product" Click="DeleteMenuItem_Click" />
                                                    <Separator />
                                                    <MenuItem Header="View Details" Click="ViewDetailsMenuItem_Click" />
                                                </ContextMenu>
                                            </Button.ContextMenu>
                                        </Button>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- Empty State -->
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <StackPanel.Visibility>
                            <MultiBinding Converter="{StaticResource ProductListEmptyConverter}">
                                <Binding Path="Products" />
                                <Binding Path="IsBusy" />
                            </MultiBinding>
                        </StackPanel.Visibility>
                        <TextBlock Text="&#x1F4E6;" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,12" />
                        <TextBlock Text="No Products Found" FontSize="18" FontWeight="Bold"
                                   Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Center" />
                        <TextBlock Text="Create your first product" FontSize="13"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   HorizontalAlignment="Center" Margin="0,4,0,16" />
                        <Button Content="+ Add Product" Command="{Binding AddProductCommand}"
                                Style="{StaticResource PrimaryCtaButtonStyle}" HorizontalAlignment="Center" />
                    </StackPanel>

                </Grid>
            </Border>

            <!-- Loading Overlay -->
            <Border Background="#CCFFFFFF" CornerRadius="12"
                    Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Border Width="36" Height="36" BorderThickness="3"
                            BorderBrush="{StaticResource AccentBrush}" CornerRadius="18"
                            RenderTransformOrigin="0.5,0.5">
                        <Border.RenderTransform>
                            <RotateTransform x:Name="SpinnerRotate" />
                        </Border.RenderTransform>
                        <Border.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="SpinnerRotate"
                                                         Storyboard.TargetProperty="Angle"
                                                         From="0" To="360" Duration="0:0:1"
                                                         RepeatBehavior="Forever" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Border.Triggers>
                    </Border>
                    <TextBlock Text="Loading products..." Margin="0,12,0,0"
                               Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center" />
                </StackPanel>
            </Border>
        </Grid>

        <!-- ===================== PAGINATION FOOTER ===================== -->
        <Grid Grid.Row="5" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" VerticalAlignment="Center"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       Text="{Binding TotalRecords, StringFormat=Total Records: {0}}" />

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Previous" Width="100" Command="{Binding PreviousPageCommand}"
                        Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,8,0" />
                <Border Background="{StaticResource BackgroundBrush}" CornerRadius="6" Padding="12,6" VerticalAlignment="Center" Margin="0,0,8,0">
                    <TextBlock FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}">
                        <TextBlock.Text>
                            <MultiBinding StringFormat="Page {0} of {1}">
                                <Binding Path="CurrentPage" />
                                <Binding Path="TotalPages" />
                            </MultiBinding>
                        </TextBlock.Text>
                    </TextBlock>
                </Border>
                <Button Content="Next" Width="100" Command="{Binding NextPageCommand}"
                        Style="{StaticResource SecondaryButtonStyle}" />
            </StackPanel>
        </Grid>

    </Grid>
</UserControl>